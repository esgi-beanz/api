server:
  port: 3000

spring:
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create

security:
  token:
    secret: ${JWT_TOKEN_SECRET}
